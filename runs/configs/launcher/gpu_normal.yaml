# @package _global_
# configs/launcher/gpu_normal.yaml

defaults:
  - override /hydra/launcher: submitit_slurm

hydra:
  launcher:
    cpus_per_task: 20
    mem_gb: 500
    partition: gpu_p
    qos: gpu_normal
    gres: gpu:1
    timeout_min: 1440
    # constraint: h100_80gb